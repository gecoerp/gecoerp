<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Una sola vista (base.view_partner_tree) alimenta Contactos, Clientes
         (Ventas) y Proveedores (Compras) — heredarla aquí cubre las tres. -->
    <record id="view_partner_tree_whatsapp_created" model="ir.ui.view">
        <field name="name">res.partner.tree.whatsapp.created</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='email']" position="after">
                <field name="whatsapp_created" string="Creado por WhatsApp"
                       widget="boolean_toggle" optional="hide"/>
            </xpath>
        </field>
    </record>

</odoo>
